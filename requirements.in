# Direct runtime dependencies. tools/lock-requirements.sh produces the hashed
# requirements.txt installed by the image. Keep version changes deliberate.
pyyaml==6.0.1
google-api-python-client==2.198.0
google-auth==2.56.3
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.1
# Shared Google/account HTTP transport, also exercised by the development gate.
requests==2.32.5
cryptography==50.0.1
# Local document conversion and card rendering; neither uses a hosted service.
firecrawl-anydoc==0.2.4
Pillow==12.3.0
